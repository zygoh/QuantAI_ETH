# QuantAI-ETH 依赖包（GPU加速版本）
# 支持CUDA 12.1，兼容RTX 4060 Ti等NVIDIA GPU
# 
# GPU安装说明：
# 1. 确保已安装NVIDIA驱动和CUDA 13.0+
# 2. 使用命令：uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 3. 验证：python -c "import torch; print(torch.cuda.is_available())"

# ========================================
# 核心框架
# ========================================
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# ========================================
# 数据库
# ========================================
asyncpg
sqlalchemy[asyncio]
redis
psycopg2-binary

# ========================================
# 机器学习（四模型集成 + GPU加速）
# ========================================
# 传统机器学习模型（支持GPU加速）
lightgbm>=4.0.0  # GPU: device='gpu'
xgboost>=1.6.0   # GPU: tree_method='hist', device='cuda' (需要1.6.0+支持callbacks参数)
catboost>=1.2.0  # GPU: task_type='GPU'

# 深度学习模型（Informer-2 + GMADL）
# 见下方深度学习框架部分

# ML工具库
scikit-learn
numpy
pandas
scipy
optuna  # 超参数优化（支持所有4个模型）
bitsandbytes #8-bit优化器

# GPU加速库（可选，用于XGBoost GPU预测避免设备不匹配警告）
# 安装命令：pip install cupy-cuda12x  # 根据CUDA版本选择（cuda11x, cuda12x等）
# cupy-cuda12x  # XGBoost GPU预测时使用，将输入数据转换到GPU
cupy-cuda12x
# 深度学习框架（神经网络 - CUDA版本）
# 注意：使用CUDA 12.1版本，与RTX 4060 Ti兼容
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1
# 安装命令：uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# ========================================
# 技术分析
# ========================================
ta

# ========================================
# 交易所API（支持Binance和OKX）
# ========================================
binance-connector  # Binance交易所API
python-okx==0.4.0  # OKX官方SDK（用于OKX交易所集成）
websocket-client  # WebSocket客户端（Binance和OKX共用）
requests  # HTTP请求库
PySocks  # SOCKS5代理支持
socksio==1.0.0
# ========================================
# 工具库
# ========================================
aiohttp
python-dateutil
pytz

# ========================================
# 系统配置说明
# ========================================
# GPU配置（config.py）：
# USE_GPU: bool = True
# GPU_DEVICE: str = "cuda:0"
#
# 支持的GPU：
# - NVIDIA RTX 4060 Ti (8GB) ✅
# - NVIDIA RTX 4070/4080/4090 ✅
# - NVIDIA RTX 3060/3070/3080 ✅
# - 其他CUDA兼容GPU ✅
#
# 性能提升：
# - Optuna优化：4.3x加速
# - 模型训练：4x加速
# - 总训练时间：130分钟 → 30分钟

